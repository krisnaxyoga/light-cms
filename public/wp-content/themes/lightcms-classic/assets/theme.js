// Enqueued through wp_enqueue_script(); lightcmsClassic comes from wp_localize_script().
document.addEventListener('DOMContentLoaded', function () {
    document.documentElement.classList.add('lightcms-classic-ready');
});
